# Lock Orientation

Perlu memanggil beberapa fungsi pada main

```dart
WidgetsFlutterBinding.ensureInitialized();
SystemChrome.setPreferredOrientations([
    DeviceOrientations.potraitUp,
]).then((fn)=>{
    runApp(MaterialApp());
})
```
